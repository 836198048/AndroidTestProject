<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fzwsc.mytestproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".kotlin1Activity"/>
        <activity android:name=".kotlin2Activity"/>
        <activity android:name=".RxJavaActivity"/>
        <activity android:name=".AnkoActivity"/>
        <!--沉浸式状态栏-->
        <activity android:name=".cjsActivity"/>
        <activity android:name=".test" />
    </application>

</manifest>